import React from 'react';
import styled from 'styled-components';

import PhotoContainer from '../views/photos/containers/PhotoContainer';

function Photo() {
  return (
    <Container>
      <PhotoContainer />
    </Container>
  );
}

const Container = styled.div`

`;

export default Photo;
